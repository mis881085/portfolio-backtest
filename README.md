# Portfolio Backtest

互動式投資組合回測工具。透過 Python 腳本從 Yahoo Finance / FinMind 抓取歷史資料，再由單頁 HTML 介面進行回測、視覺化與比較。

線上版本：**https://mis881085.github.io/portfolio-backtest/**

---

## 功能

### 回測
- 支援台股（.TW）與美股混合組合；純台股組合不需要匯率資料
- 年末再平衡策略（期初 All-in，每年 12 月底依目標比例再投入）
- 考慮換匯價差、台股手續費與交易稅、美股配息預扣稅、複委託費用
- 互動式淨值走勢圖（可縮放、平移）
- 績效摘要：年化報酬、年化標準差、最大跌幅
- 各年度報酬率表（hover 顯示再平衡明細）

### 蒙地卡羅退休模擬
- 基於各標的歷史年化報酬率，以多變量常態分佈（Cholesky 分解）取樣，保留資產間相關性
- 美股資產自動考慮 USD/TWD 匯率的聯合分佈
- 提領模型：年初先提領，再投資剩餘資產，通膨以常態分佈隨機取樣
- 輸出退休成功率、殘值分佈（P5 / P25 / P50 / P75 / P95）、殘值 box plot
- 顯示各資產間相關係數矩陣與歷史年化均值／標準差
- **資料品質警告**：
  - 共同歷史年份不含 2008 年金融海嘯時，顯示尾部風險低估警示
  - 共同歷史年份不足 15 年時，顯示樣本偏向特定市況警示
- 自動排除不完整的當年度資料（非 12 月結尾），避免 YTD 報酬被當作全年報酬計算

### 歷史退休情境模擬
- 以真實歷史報酬（非隨機抽樣）逐年模擬退休資產變化
- 年初提領 → 剩餘資金投資一整年 → 年末再平衡至目標比重
- 每年顯示：年度提領、各標的 TWD 報酬與再平衡買賣方向、組合報酬率、年末資產
- 通膨按設定均值逐年調升提領額
- 若投資組合歷史資料不足以涵蓋指定起始年，自動顯示「無法模擬」

---

## 安裝套件

```bash
pip install yfinance pandas numpy requests
```

---

## 使用方式

### 1. 匯出資料

```bash
python export_data.py <ticker1> <ticker2> ... --out <輸出目錄>
```

**範例：**

```bash
# 台股 ETF
python export_data.py 0050.TW 006208.TW --out data

# 美股 ETF
python export_data.py VT VTI SPY BNDW --out data

# 混合（台美股 + 指定輸出目錄）
python export_data.py 0050.TW 006208.TW VT VTI SPY BNDW --out data

# 個股
python export_data.py 2880.TW 2892.TW --out data

# 指定起始年份
python export_data.py 0050.TW --start 2000-01-01 --out data
```

執行後會在指定目錄產生：
- `data_<ticker>.json`：各標的歷史收盤價與股息
- `data_FX_USDTWD.json`：USD/TWD 匯率歷史資料

> **台股資料來源**：腳本會自動同時嘗試 yfinance 與 FinMind（無需 token），比較兩者歷史年跨度，選擇較早的來源輸出。FinMind 有速率限制，每支台股約需額外 7 秒。

> **注意**：台股代號須補前導零，例如 `006208.TW`、`00878.TW`，而非 `6208.TW`。

### 2. 開啟 index.html

直接在瀏覽器開啟 `index.html`，或使用線上版本：
https://mis881085.github.io/portfolio-backtest/

**操作步驟：**

1. **載入資料**：將 `data/` 資料夾內的 JSON 檔案拖放至上傳區（可一次多選），或從瀏覽器直接拖入 raw 檔案連結
2. **設定投資組合**：新增投資組合，選擇標的與各標的比重（須加總為 100%）
3. **調整回測參數**：期初金額、手續費、稅率等（可依需求展開修改）
4. **執行回測**：點擊「執行回測」，結果自動顯示於下方
5. **蒙地卡羅模擬**：設定退休年限、模擬次數、每月提領金額與通膨參數，點擊「執行退休模擬」
6. **歷史情境模擬**：設定退休起始年，點擊「執行歷史情境模擬（真實數據）」，查看逐年詳細資產變化

### 蒙地卡羅參數說明

| 參數 | 說明 |
|------|------|
| 退休總年限 | 模擬需撐過的年數（例如 45 年） |
| 模擬次數 | 隨機抽樣次數，建議 10,000 以上；次數越多結果越穩定 |
| 退休首年每月提領 | 以今日購買力計算的月提領金額（台幣） |
| 通膨均值 | 每年通膨率的期望值（建議 2～3%） |
| 通膨標準差 | 通膨的年度波動幅度（建議 1～2%） |

> **提領本金**：使用「期初金額」欄位的值作為退休起始資產，與回測共用同一個設定。

---

## 已知問題

- **台灣債券 ETF**：帶有 `B` 字尾的台灣債券 ETF（如 `00933B.TW`、`00679B.TW`）在 Yahoo Finance 與 FinMind 均無資料，`export_data.py` 無法下載。如需使用此類標的，須自行從其他來源（如證交所、CMoney）取得日線資料，並手動整理成以下 JSON 格式：

```json
{
  "ticker": "00933B.TW",
  "currency": "TWD",
  "generated": "2026-06-24",
  "data": [
    { "date": "2022-06-01", "close": 10.00, "div": 0.0 },
    { "date": "2022-06-02", "close": 10.02, "div": 0.0 }
  ]
}
```

- **USD/TWD 匯率歷史資料**：Yahoo Finance 對 `TWD=X` 最早僅提供至 2004 年左右；若需 2003 年以前的台美混合組合回測，匯率資料將無法覆蓋。純台股組合不受此限制。
